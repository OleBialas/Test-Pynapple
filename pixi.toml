[workspace]
authors = ["obi <ole.bialas@posteo.de>"]
channels = ["conda-forge"]
name = "Test-Pynapple"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
install-kernel = "python -m ipykernel install --name 'pynapple' --user --display-name 'pynapple'"

[dependencies]
python = ">=3.14.6,<3.15"
ipykernel = ">=7.3.0,<8"

[pypi-dependencies]
pynapple = ">=0.11.3, <0.12"
